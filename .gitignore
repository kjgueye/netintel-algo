# deps & build
node_modules/
dist/

# local env — the real .env (with PAYTO/network) must never be committed
.env
.env.*
!.env.example

# logs & scratch
*.log
.DS_Store

# throwaway local tooling — key derivation + one-time payers. Never commit these
# (they touch mnemonics/keys and are not part of the deployed service).
# scripts/sync-from-netintel.ts is deliberately NOT ignored: it handles no keys
# and is how src/routes + src/utils are kept in step with the NetIntel repo.
scripts/hd-scan.ts
scripts/pay-test.ts
scripts/seed-all.ts
